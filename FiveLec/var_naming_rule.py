# Variable Naming Rules
'''
### ✅ **Variable Naming Rules in Python**  

1. **Only Letters, Digits, and Underscore (`_`) are allowed**  
   - ✅ `name`, `user_age`, `total_price`  
   - ❌ `user-name` (hyphen not allowed)  
   - ❌ `my name` (spaces not allowed)  

2. **Cannot Start with a Digit**  
   - ✅ `name1`, `age_25`  
   - ❌ `1name`, `3total`  

3. **Case-Sensitive (Uppercase & Lowercase are Different)**  
   - ✅ `name` and `Name` are different variables  

4. **Cannot Use Python Reserved Keywords**  
   - ❌ `if = 5`, `class = "Python"`  
   - ✅ `if_ = 5`, `class_ = "Python"` (Using `_` is a workaround)  

5. **Use Meaningful and Descriptive Names**  
   - ✅ `total_price`, `student_name`  
   - ❌ `x`, `y`, `z` (unless necessary)  

6. **Preferred Naming Style in Python is Snake Case (`snake_case`)**  
   - ✅ `user_name`, `product_price`  

7. **Camel Case and Pascal Case (Not Recommended in Python)**  
   - ✅ `camelCaseExample` (JavaScript Style)  
   - ✅ `PascalCaseExample` (Used for class names)  

8. **Underscore (`_`) Usage**  
   - `_name` → Private variable convention  
   - `__name__` → Dunder (double underscore) used for special Python variables  

9. **Avoid Single Character Names (Unless in Loops or Math Expressions)**  
   - ✅ `index`, `counter`  
   - ❌ `i`, `j`, `k` (unless in loops)  

10. **Constants Should be in Uppercase**  
   - ✅ `PI = 3.14`, `MAX_USERS = 100`  


'''